<div>
	<h5><%= comment.author_name %></h5>
	<p class="comment"><%= comment.body %></p>
	<h6 style="font-size: 12px">Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later </h6>
	<p style="font-size: 11px"><%= comment.created_at %></p>
</div>